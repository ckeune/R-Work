# # # # # # # # # # # # # # # #
library(downloader)
setwd( "C:/My Directory/NLS/" )
source_url( "https://raw.githubusercontent.com/ajdamico/usgsd/master/National%20Longitudinal%20Surveys/download%20all%20microdata.R" , prompt = FALSE , echo = TRUE )
install.packages(downloader)
install.packages("downloader")
library(downloader)
setwd( "C:/My Directory/NLS/" )
source_url( "https://raw.githubusercontent.com/ajdamico/usgsd/master/National%20Longitudinal%20Surveys/download%20all%20microdata.R" , prompt = FALSE , echo = TRUE )
setwd( "C:/R/NLS/" )
required_libraries <- c('ggplot2',
'reshape2',
'stringr',
'lubridate',
'dplyr',
'scales',
'httr',
'downloader')
for (lib_name in required_libraries){
if (!require(lib_name, character.only = T))
install.packages(lib_name)
library(lib_name, c
}
required_libraries <- c('ggplot2',
'reshape2',
'stringr',
'lubridate',
'dplyr',
'scales',
'httr',
'downloader')
for (lib_name in required_libraries){
if (!require(lib_name, character.only = T))
install.packages(lib_name)
library(lib_name, character.only = T)
}
library("downloader", lib.loc="~/R/R-3.1.2/library")
library("RCurl", lib.loc="~/R/R-3.1.2/library")
library("xtable", lib.loc="~/R/R-3.1.2/library")
install.packages(c("BH", "digest", "dplyr", "foreign", "ggm", "lazyeval", "magrittr", "manipulate", "mgcv", "RColorBrewer", "reshape2"))
source_url( "https://raw.githubusercontent.com/ajdamico/usgsd/master/National%20Longitudinal%20Surveys/download%20all%20microdata.R" , prompt = FALSE , echo = TRUE )
setwd( "C:/R/NLS/" )
library(httr)  	# load httr package (downloads files from the web, with SSL and cookies)
library(XML)		# load XML (parses through html code to extract links)
library(stringr)	# load stringr package (manipulates character strings easily)
library(downloader)	# downloads and then runs the source() function on scripts from github
library(httr)  	# load httr package (downloads files from the web, with SSL and cookies)
library(XML)		# load XML (parses through html code to extract links)
required_libraries <- c('ggplot2',
'reshape2',
'stringr',
'lubridate',
'dplyr',
'scales',
'httr',
'downloader',
'XML',
'stringer')
for (lib_name in required_libraries){
if (!require(lib_name, character.only = T))
install.packages(lib_name)
library(lib_name, character.only = T)
}
required_libraries <- c('ggplot2',
'reshape2',
'stringr',
'lubridate',
'dplyr',
'scales',
'httr',
'downloader',
'XML',
'stringer')
for (lib_name in required_libraries){
if (!require(lib_name, character.only = T))
install.packages(lib_name)
library(lib_name, character.only = T)
}
library("ggplot2", lib.loc="~/R/R-3.1.2/library")
detach("package:ggplot2", unload=TRUE)
library("ggplot2", lib.loc="~/R/R-3.1.2/library")
required_libraries <- c('proto',
'ggplot2',
'reshape2',
'stringr',
'lubridate',
'dplyr',
'scales',
'httr',
'downloader',
'XML',
'stringer')
for (lib_name in required_libraries){
if (!require(lib_name, character.only = T))
install.packages(lib_name)
library(lib_name, character.only = T)
}
library("ggplot2", lib.loc="~/R/R-3.1.2/library")
install.packages("proto")
install.packages("proto")
required_libraries <- c('proto',
'ggplot2',
'reshape2',
'stringr',
'lubridate',
'dplyr',
'scales',
'httr',
'downloader',
'XML',
'stringer')
for (lib_name in required_libraries){
if (!require(lib_name, character.only = T))
install.packages(lib_name)
library(lib_name, character.only = T)
}
required_libraries <- c('proto',
'reshape2',
'stringr',
'lubridate',
'dplyr',
'scales',
'httr',
'downloader',
'XML',
'stringer')
for (lib_name in required_libraries){
if (!require(lib_name, character.only = T))
install.packages(lib_name)
library(lib_name, character.only = T)
}
required_libraries <- c('proto',
'stringr',
'lubridate',
'dplyr',
'scales',
'httr',
'downloader',
'XML',
'stringer')
for (lib_name in required_libraries){
if (!require(lib_name, character.only = T))
install.packages(lib_name)
library(lib_name, character.only = T)
}
library("lubridate", lib.loc="~/R/R-3.1.2/library")
detach("package:lubridate", unload=TRUE)
library("lubridate", lib.loc="~/R/R-3.1.2/library")
install.packages("lubridate")
required_libraries <- c('proto',
'stringr',
'lubridate',
'dplyr',
'scales',
'httr',
'downloader',
'XML',
'stringer')
for (lib_name in required_libraries){
if (!require(lib_name, character.only = T))
install.packages(lib_name)
library(lib_name, character.only = T)
}
library("lubridate", lib.loc="~/R/R-3.1.2/library")
detach("package:lubridate", unload=TRUE)
required_libraries <- c('proto',
'stringr',
'dplyr',
'scales',
'httr',
'downloader',
'XML',
'stringer')
for (lib_name in required_libraries){
if (!require(lib_name, character.only = T))
install.packages(lib_name)
library(lib_name, character.only = T)
}
library("downloader", lib.loc="~/R/R-3.1.2/library")
install.packages("downloader")
library("downloader", lib.loc="~/R/R-3.1.2/library")
detach("package:downloader", unload=TRUE)
